name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install package and ghdag
        run: |
          pip install -e ".[dev]"
          pip install "ghdag @ git+https://github.com/sumipan/ghdag.git@v0.35.0"
      - name: Ruff
        run: ruff check src/ tests/
      - name: Pytest
        run: pytest -q
